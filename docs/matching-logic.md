# Matching Logic

Given extracted documents and user inputs (permit type, employment status, EU status), `matchBank` in `src/lib/matching.ts` returns `MatchResult` per bank.

Ready = no missing required docs AND no warnings about permit acceptance.
